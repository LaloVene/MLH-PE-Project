import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'MLH-PE-Project',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
